#!/bin/sh

# ------------------------------------------------------------------------------
# create auxiliary folders
# ------------------------------------------------------------------------------

[ ! -d ./.config ] && mkdir -p ./.config || true
# [ ! -d ./.packages ] && mkdir -p ./.packages || true

# ln -sfn /home/mpenna/Projects/projects-095-defog-aceleracao-mvp/packages/amazon-sp-api ./.packages/amazon-sp-api
